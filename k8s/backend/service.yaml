apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: car-vault
spec:
  selector:
    app: car-vault-backend
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
  type: ClusterIP    # internal only — frontend reaches it as http://backend-service:3001
